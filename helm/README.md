## DevBox Helm chart

```
$ helm repo add devbox https://aidbox.github.io/devbox
"devbox" has been added to your repositories
$ helm repo list
NAME            URL
devbox          https://aidbox.github.io/devbox

$ helm install -i mybox devbox/devbox
```

